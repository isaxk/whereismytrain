import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.isaxk.whereismytrain',
  appName: 'WhereIsMyTrain?',
  webDir: 'dist'
};

export default config;
